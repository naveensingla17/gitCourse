Q1. Can you restore, deleted branch ?
Ans: No, but you can refer details or history using git reflog, git log, git show, etc.

Q2. Difference in below commands
  git config --global
  git config --local
  git config --system

Q3. Describe different stages in git
Ans: Workspace - before git add command
Staging Area - Modified but not commit
Local Repo - After commit but on local repo not online yet as not done git push yet

Q4. What will happen when you run git init ?
Ans: .git folder will be created

Q5. Use and benefit of git ?
Ans: 
1. Collabrator development or Source code management
2. VCS (Version control)
3. Branches and merge
4. VCS tracking - Hash ID
5. Security 
    SHA1
    Checksum
6. Integration
    GitHub / GitLab
    CI / CD
    Github Actions
    Webhooks

Q6. Sometime branches in name as master or main, why is that ?
Ans: public git - master
private git - main

Q7. Latest version of git ?
Ans: 2.50.0

Q8. How do you undo from stage area to workspace ?
Ans: git rm --cached <filenam>  # for first time

Stage --> Workspace (Restore)
git restore --staged <filename>

Local Repo --> Workspace (Revert)

Q9. Can we do git checkout on commit id ?
Ans: Yes
git checkout <Commit ID>

Q10. How do you create branch
Ans:
git checkout -b feat/cgc-0000/update-threshold
git branch feat/cgc-0000/update-threshold

Q11. How do you check logs ?
Ans: git log
git log --oneline
git show <commmit id>
git reflog

Q12. You added a file but git not processing it, what can be the issue ?
Ans: .gitignore

Q13. How to rename branch say "master" branch ?
Ans: git branch -M master

Q14: What for "git remote" used?
Ans: git remote show origin
git remote 

Q15: git push
Ans: git push origin master

Q16: diff in git branch and git branch -r
Ans:

Q17: 

// Linux
yum install git -y 

//ubuntu
apt install git

// git version
git --version

// mkdir /opt/myreop
cd /opt/myrepo

git init

tree .git

git config --global user.name "Naveen"
git config --global user.email "naveen@gmail.com"
git config --list

===> working directory --> git add --> staging area --> git commit --> local repo --> git push --> remote repo --> git pull --> working directory


//git checkout
git checkout -b feat/CGC-0000/this-is-my-first-commit
git checkout <commit id>   #if you want to switch to lower commit id and undo changes


// git add --> to move file to stage area
git add . ## all files
git add filename
git add -A
git status
git status -u

// undo command to move back to working director i.e untracked
git rm --cached
git revert (local repo to workspace)
git restore (Stage to workspace)

git restore --staged <filename>

git revert <commit id>
git revert --abort

git restore --> move file from staging to work space i.e after git addd
whereas git revert --> moves file from local repo to work space i.e after git commit


// git commmit
git commit -m "this is my first commit"
or git commit -m "feat(CW):CGC:0000 this is my first commit"

git commit -am "this is my first commit" --> add and commit in single command

git commit -m "this is single file commit" <filename>


// show all branched on remote end
git remote -y
git remote -v
git remote set-url origin <git-hub/lsb-dir-location> # SSH path

// git log
git log
git log --oneline
git log -1

git log --oneline --decorate --graph --all

git show <commit id>
git show origin
git show-branch

git remote show origin

git reflog   # provides complete details where to where what done

// git ignore
.gitignore    # has extension which is not allowed

//git push


// git clone


//git pull

//git push

//git fetch vc git pull
pull remote repo to local repo
whereas git pull --> remote repo to working directory
//git merge
      conflict

git merge --no-ff

git branch -a
git branch -r

//git revert

//git restore
git restore --staged about.html

//git reset
  --soft
  --mixed (default)
  --hard

git reset --soft <commit id>

git clean -n
git clean -f

git branch -d (delete branch should be merged)
git branch -D (forcefully delete)

// git stash (temparory memory storage)
git stash list
git stash
git stash apply <stash id>
git stash drop stash{2}

//git rebase
to add other branch history or commit id's into new branch

//git cherry-pick

// git rm
git mv

//forking

